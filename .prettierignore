# Build and generated output
.next/
out/
build/
coverage/
playwright-report/
playwright-report-auth-disabled/
playwright-report-auth-rate-limit/
test-results/
next-env.d.ts
pnpm-lock.yaml

# Data files are managed by the Python tooling and must not be reformatted
data/
docs/manual-review-required.md
docs/.arabic-placeholders.json

# Generated content-release artifacts (deterministic bytes; never reformat)
public/content/
content-server/

# Drizzle-generated migrations and journal/snapshot metadata (Phase 15) —
# regenerated verbatim by `drizzle-kit generate`; reformatting them would
# just be undone (or drift) on the next regeneration
db/migrations/

# Python
scripts/
__pycache__/

# Phase-workflow runtime state and review reports (machine-written)
.claude/review/

# User-authored phase specification prompts (authored outside repo style)
docs/phases/
