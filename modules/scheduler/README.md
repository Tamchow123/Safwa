# modules/scheduler

Future home of the pure-TypeScript scheduling logic (Phase 7): ts-fsrs
integration, rating mapping, review-event creation with causal lineage,
due-item selection and learner-state projection. This module must never
import React, DOM APIs or database clients (see `docs/ARCHITECTURE.md` §2
and `docs/OFFLINE_AND_SYNC.md` §5).

No implementation exists yet by design (Phase 1 creates boundaries only).
